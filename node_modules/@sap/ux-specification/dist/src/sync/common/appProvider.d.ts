import type { ExtensionLogger, Application, Files, Manifest, PagesBase, v2, v4 } from '@sap/ux-specification-types';
import { FioriElementsVersion } from '@sap/ux-specification-types';
export interface Controllers {
    [key: string]: string;
}
export declare abstract class AppProvider {
    readonly app: Application;
    readonly logger: ExtensionLogger;
    readonly schemas: Files;
    unhandledControllers: Array<string>;
    protected constructor(manifest: Manifest, schemas: Files, home: string, pages: PagesBase, version: FioriElementsVersion, settings?: v2.AppSettings | v4.AppSettings, logger?: ExtensionLogger);
    /**
     * Create config files for app (app.json) and pages (pages/{name}.json).
     * This is basically a copy of createFiles optimized to be used in FTFS:
     * it always uses posix path notation with forward slash (/)
     * @param {ConvertedMetadata} oDataServiceAVT - Complete service information, as returned by annotation vocabularies tool
     * @returns app and page files as separate objects
     */
    createConfigFiles(): {
        appConfig: Files;
        pageConfigs: Files;
    };
}
//# sourceMappingURL=appProvider.d.ts.map